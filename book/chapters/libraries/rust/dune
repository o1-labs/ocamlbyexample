(dirs :standard \ target)

(rule
 (target librust_thing.a)
 (action
  (progn
   (run cargo build --release)
   (run cp target/release/%{target} .))))

(library
 (name lib)
 (foreign_archives rust_thing)
 (c_library_flags
  (-lpthread -lc -lm)))
